const pingHandler = require('./pingHandler');
const mailHandler = require('./mailHandler');
const singleMailHandler = require('./singleMailHandler');

module.exports = {
  pingHandler,
  mailHandler,
  singleMailHandler,
};